apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: dmz-network
spec:
  config: '{
      "cniVersion": "0.3.1",
      "name": "dmz-network",
      "type": "bridge",
      "bridge": "br-dmz",
      "ipam": {}
    }'
